meta {
  name: Delete Agent
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/agents/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <agent-uuid>
}
