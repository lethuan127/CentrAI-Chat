meta {
  name: Create Provider
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/providers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "OpenAI",
    "type": "OPENAI",
    "apiKey": "sk-...",
    "isEnabled": true
  }
}
