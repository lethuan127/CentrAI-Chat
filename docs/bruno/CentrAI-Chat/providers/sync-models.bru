meta {
  name: Sync Models
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/providers/:id/sync
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <provider-uuid>
}
