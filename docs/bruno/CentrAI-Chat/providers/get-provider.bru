meta {
  name: Get Provider
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/providers/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <provider-uuid>
}
