meta {
  name: Get Messages
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/chat/conversations/:id/messages?page=1&limit=50
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <conversation-uuid>
}
