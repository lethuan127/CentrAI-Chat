meta {
  name: Delete Conversation
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/chat/conversations/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <conversation-uuid>
}
