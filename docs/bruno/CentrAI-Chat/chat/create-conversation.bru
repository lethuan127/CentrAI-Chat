meta {
  name: Create Conversation
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/chat/conversations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "Test Conversation"
  }
}
