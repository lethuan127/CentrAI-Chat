meta {
  name: Update Settings
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/admin/settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "registrationEnabled": true,
    "rateLimitPerMinute": 60
  }
}
