meta {
  name: Update User
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/admin/users/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <user-uuid>
}

body:json {
  {
    "role": "DEVELOPER"
  }
}
